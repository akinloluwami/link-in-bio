export interface LinkProps {
  url: string;
  title: string;
}

export interface ProfileProps {
  name: string;
  bio: string;
  image: string;
}

export interface SocialLinkProps {
  url: string;
  platform: string;
}
